# Set git config
git config --global user.name "PavanMadde"
git config --global user.email "pavanmadde1998@gmail.com"
git config --global --list

# create a new repository on the command line
echo "# welch-plug-demo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/PavanLogs/welch-plug-demo.git
git push -u origin main

# push an existing repository from the command line
git remote add origin https://github.com/PavanLogs/welch-plug-demo.git
git branch -M main
git push -u origin main
